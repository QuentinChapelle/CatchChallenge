{% extends '@Gamer/base.html.twig' %}

{% block content %}
    <div class="container well">
        <div class="row">
            <div class="col-xs-12">
                <h2 class="text-center">Validation</h2>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-6">
                <h3 class="text-center">Votre proposition de photos</h3>
                {{ form_start(form) }}
                {{ form_row(form.photo) }}
                <input type="submit" value="create"/>
                {{ form_end(form) }}
            </div>
            <div class="col-xs-6">
                <h3 class="text-center">Photo à trouver</h3>
                {% for image in partie.images %}
                    <img class="img-rounded center-block" src="{{ asset('/uploads/partie_picture/'~ image.photo )}}"/>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
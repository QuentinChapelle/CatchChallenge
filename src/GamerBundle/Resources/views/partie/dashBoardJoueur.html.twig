{% extends '@Gamer/base.html.twig' %}

{% block content %}
    <div class="container well">
        <div class="row">
            <div class="col-xs-12">
                <h2 class="text-center">Validation</h2>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-6">
                {{ form_row(form.photo) }}
                    <input type="submit" value="Create" href="{{ path('partie_index') }}"/>
            </div>
            <div class="col-xs-6">
                <div class="col-xs-6">
                    <img src="{{ asset('assets/images/chatcorn.png') }}" class="center-block img-responsive">
                </div>
                {#{% for image in partie.images %}#}
                    {#<img class="img-rounded" src="{{ asset('/uploads/partie_picture/'~ image.photo )}}"/>#}
                {#{% endfor %}#}
            </div>
        </div>
        <div class="row">
            <div class="col-xs-6">
                <input type="button" value="refuser la photo" class="btn-lg btn-danger center-block">
            </div>
            <div class="col-xs-6">
                <input type="button" value="valider la photo" class=" btn-lg btn-success center-block">
            </div>
        </div>
    </div>
{% endblock %}